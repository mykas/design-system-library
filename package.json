{
  "name": "design-system-library",
  "private": true,
  "packageManager": "yarn@3.3.1",
  "scripts": {},
  "lint-staged": {
    "package.json": "yarn syncpack format",
    "yarn.lock": "yarn dedupe"
  },
  "devDependencies": {
    "husky": "^8.0.2",
    "lint-staged": "^12.5.0",
    "syncpack": "^7.2.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
